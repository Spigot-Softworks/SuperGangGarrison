name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore .NET workloads
        run: dotnet workload restore OpenGarrison.sln

      - name: Restore solution
        run: dotnet restore OpenGarrison.sln

      - name: Verify repository policies
        shell: pwsh
        run: |
          ./scripts/verify-repository-root-layout.ps1
          ./scripts/verify-gameplay-pack-layout.ps1
          ./scripts/verify-packaged-content.ps1 -SelfTest
          dotnet run --project Tools/OpenGarrison.PluginHost.ApiGenerator/OpenGarrison.PluginHost.ApiGenerator.csproj -- --check

      - name: Restore browser build graph
        run: >
          dotnet restore Client.Browser/OpenGarrison.Client.Browser.csproj
          -p:OpenGarrisonClientAsLibrary=true

      - name: Build solution
        run: >
          dotnet build OpenGarrison.sln
          --configuration Debug
          --no-restore
          -p:EnableMGCBItems=false

      - name: Run networking tests
        run: >
          dotnet test Tests/OpenGarrison.Networking.Tests/OpenGarrison.Networking.Tests.csproj
          --configuration Debug
          --no-build
          --no-restore

      - name: Run gameplay and plugin-host tests
        run: >
          dotnet test Tests/OpenGarrison.PluginHost.Tests/OpenGarrison.PluginHost.Tests.csproj
          --configuration Debug
          --no-build
          --no-restore
