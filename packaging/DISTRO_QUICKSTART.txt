Building release packages

Run commands from the repository root. Version 1.0.2 below is an example;
choose the version for your release.

windows only:
powershell -ExecutionPolicy Bypass -File .\scripts\package.ps1 -Platforms win-x64

linux only:
pwsh ./scripts/package.ps1 -Platforms linux-x64

windows only with release version:
powershell -ExecutionPolicy Bypass -File .\scripts\package.ps1 -Platforms win-x64 -Version 1.0.2

linux only with release version:
pwsh ./scripts/package.ps1 -Platforms linux-x64 -Version 1.0.2

- Linux packaging bundles libmsquic from the build host. Install it before packaging with `sudo apt-get install libmsquic`; cross-builds can pass `-LinuxMsQuicLibraryPath` with a compatible Linux library file.
- To rebuild a Linux package while reusing an existing packaged browser atlas, pass `-ReusePackagedAtlasFrom`, for example: `pwsh ./scripts/package.ps1 -Platforms linux-x64 -Version 1.0.2 -ReusePackagedAtlasFrom ./dist/linux-x64/app/Content/Browser`.

macOS only:
pwsh ./scripts/package.ps1 -Platforms osx-x64,osx-arm64

CI/local bash wrapper:
bash ./scripts/build.sh win-x64

output:
dist\<runtime-id>\
dist\OpenGarrison-*.zip
dist\OpenGarrison-*.tar.gz

notes:
- the package script stages publishes under `dist\_staging` before copying the finished package into `dist\<runtime-id>`
- the package includes client/server content, default config files, `sampleMapRotation.txt`, packaging README, and bundled server plugins
- the package writes `version.txt` at the package root; pass `-Version` for release builds so clients can compare updates reliably
- when upgrading legacy packages without version.txt, choose a version greater than 1.0.0; those packages may report the SDK default version
- packaged clients should start with `Super Gang Garrison.exe` on Windows or `./OG2` / `sh run-client.sh` on Linux; that entrypoint checks the platform update manifest, then starts `OG2.Game.exe` / `OG2.Game`
- if a Linux archive was built or extracted without executable bits, start with `sh run-client.sh`
- packaging performs restore/publish only; verification happens separately from this script
